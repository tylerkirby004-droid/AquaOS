# AI experiments

AI workloads run in an isolated VM or container and consume historical telemetry/camera data. Their output is advisory and should publish only recommendations under `aquaos/home/reef/analysis/`.
