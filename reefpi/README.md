# Reef-Pi integration

Reef-Pi remains the equipment authority. Record controller exports, channel mappings, sensor calibration, local schedules, and network configuration here without committing credentials.

An MQTT bridge should publish the normalized contract in `docs/mqtt-topics.md` and reject unsafe/unrecognized commands.
