# Home Assistant

Deploy Home Assistant OS as its own Proxmox VM. Keep UI dashboards, notifications, and integrations here. Store exported dashboard/package configuration in this directory after validation.

Home Assistant may issue convenience commands only through the guarded MQTT command contract; it is not the safety controller.
