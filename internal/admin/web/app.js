const token=document.querySelector('#token');const result=document.querySelector('#result');const connection=document.querySelector('#connection');
async function call(path,options={}){const headers={Authorization:`Bearer ${token.value}`,'Content-Type':'application/json',...(options.headers||{})};const response=await fetch(path,{...options,headers});if(!response.ok)throw new Error((await response.json()).detail||response.statusText);return response}
document.querySelector('#connect').onclick=async()=>{try{const response=await call('/api/status');result.textContent=JSON.stringify(await response.json(),null,2);connection.textContent='Authenticated'}catch(error){connection.textContent=error.message}};
document.querySelectorAll('[data-action]').forEach(button=>button.onclick=async()=>{try{const action=button.dataset.action;if(action==='backup'){const response=await call('/api/backup');const link=document.createElement('a');link.href=URL.createObjectURL(await response.blob());link.download='aquaos-backup.zip';link.click();return}const method=action==='status'?'GET':'POST';const path=`/api/${action}`;const body=method==='POST'?JSON.stringify({dryRun:true}):undefined;const response=await call(path,{method,body});result.textContent=JSON.stringify(await response.json(),null,2)}catch(error){result.textContent=error.message}});
for(const [id,path] of [['validate','validate'],['apply','apply']])document.querySelector(`#${id}`).onclick=async()=>{try{const response=await call(`/api/config/${path}`,{method:'POST',headers:{'Content-Type':'application/yaml'},body:document.querySelector('#candidate').value});result.textContent=JSON.stringify(await response.json(),null,2)}catch(error){result.textContent=error.message}};
